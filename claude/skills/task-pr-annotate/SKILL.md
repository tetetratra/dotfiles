---
name: PR補足情報追加
description: PRにインラインコメントで補足情報を追加し、レビュワーがレビューしやすくします（コードの呼び出し元、実装方式の選択理由、変更範囲の妥当性など）。前段：PR作成（task-write-pr）。後段：PRフォローアップ（task-pr-followup）またはレビュー待ち。
---

`/task-pr-annotate` コマンドを実行してください。

コマンドの引数には、対象PRのURLまたは番号を渡してください。
